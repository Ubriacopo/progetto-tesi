base_path: /mnt/turing-datasets/

defaults:
  - hydra-setup
  - dataset: ???
  - preprocessing: ???

hydra:
  job:
    name: preprocessing

  run:
    dir: outputs/${dataset.name}/${hydra.job.name}

dataset:
  eeg_config:
    model_weights_path: /root/PycharmProjects/progetto-tesi/main/dependencies/cbramod/pretrained_weights.pth