base_path: /mnt/datasets/
defaults:
  - hydra-setup
  - dataset: ???
  - preprocessing: ???

hydra:
  job:
    name: preprocessing
  run:
    dir: outputs/${hydra.job.name}/${dataset.name}/${now:%Y-%m-%d_%H-%M-%S}

dataset:
  eeg_config:
    model_weights_path: /home/jfichera/PycharmProjects/progetto-tesi/main/dependencies/cbramod/pretrained_weights.pth