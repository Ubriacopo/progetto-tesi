base_path: /mnt/pve/datasets_jfichera/
defaults:
  - hydra-setup
  - dataset: ???
  - preprocessing: ??? # todo cambia
  - segmenter: base_segmenter

transcript_audio: false

hydra:
  job:
    name: intervals-extractor

  run:
    dir: outputs/${hydra.job.name}/${dataset.name}/${now:%Y-%m-%d_%H-%M-%S}