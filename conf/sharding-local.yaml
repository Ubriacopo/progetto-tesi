base_path: /home/jacopo/dataset/EEGAVI/
defaults:
  - hydra-setup
  - sharding: ???

hydra:
  job:
    name: preprocessing

  run:
    dir: outputs/${hydra.job.name}/${sharding.name}/${now:%Y-%m-%d_%H-%M-%S}

dataset:
  eeg_config:
    model_weights_path: /home/jacopo/PycharmProjects/progetto-tesi/main/dependencies/cbramod/pretrained_weights.pth