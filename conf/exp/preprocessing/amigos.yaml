# @package _global_
defaults:
  - override /dataset: amigos
  - override /preprocessing: interleaved

base_path: /mnt/pve/datasets_jfichera/
dataset:
  uid_store_path: ${base_path}EEGAVI/DREAMER-TOY/uid_store.csv

preprocessing:
  output_path: ${base_path}EEGAVI/DREAMER-TOY/${.out_folder_name}/

