# @package _global_
defaults:
  - override /dataset: deap
  - override /preprocessing: interleaved-downstream

dataset:
  uid_store_path: ${base_path}EEGAVI/uid_store.csv

preprocessing:
  output_path: ${base_path}EEGAVI/DOWNSTREAM/${.out_folder_name}-deap/

base_path: /mnt/pve/datasets_jfichera/