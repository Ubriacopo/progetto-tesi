# @package _global_
defaults:
  - override /dataset: faced
  - override /preprocessing: interleaved

dataset:
  uid_store_path: ${base_path}EEGAVI/FACED-PROBE/uid_store.csv

preprocessing:
  output_path: ${base_path}EEGAVI/FACED-PROBE/${.out_folder_name}/

base_path: /home/jacopo/dataset/