#
defaults:
  - template
  - factory: best_eeg_inter_avi
  - pivot: eeg_default
  # Supports mappings
  - supports@supports.vid: vid_default
  - supports@supports.aud: aud_default
  - supports@supports.txt: txt_default
  - supports@supports.ecg: ecg_default
